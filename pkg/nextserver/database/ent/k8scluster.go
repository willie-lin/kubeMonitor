// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/willie-lin/kubeMonitor/pkg/nextserver/database/ent/k8scluster"
)

// K8sCluster is the model entity for the K8sCluster schema.
type K8sCluster struct {
	config `json:"-"`
	// ID of the ent.
	ID uint `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt time.Time `json:"deleted_at,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// AgentClusterId holds the value of the "agentClusterId" field.
	AgentClusterId uint `json:"agentClusterId,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*K8sCluster) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case k8scluster.FieldID, k8scluster.FieldAgentClusterId:
			values[i] = new(sql.NullInt64)
		case k8scluster.FieldName:
			values[i] = new(sql.NullString)
		case k8scluster.FieldCreatedAt, k8scluster.FieldUpdatedAt, k8scluster.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type K8sCluster", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the K8sCluster fields.
func (kc *K8sCluster) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case k8scluster.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			kc.ID = uint(value.Int64)
		case k8scluster.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				kc.CreatedAt = value.Time
			}
		case k8scluster.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				kc.UpdatedAt = value.Time
			}
		case k8scluster.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				kc.DeletedAt = value.Time
			}
		case k8scluster.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				kc.Name = value.String
			}
		case k8scluster.FieldAgentClusterId:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field agentClusterId", values[i])
			} else if value.Valid {
				kc.AgentClusterId = uint(value.Int64)
			}
		}
	}
	return nil
}

// Update returns a builder for updating this K8sCluster.
// Note that you need to call K8sCluster.Unwrap() before calling this method if this K8sCluster
// was returned from a transaction, and the transaction was committed or rolled back.
func (kc *K8sCluster) Update() *K8sClusterUpdateOne {
	return (&K8sClusterClient{config: kc.config}).UpdateOne(kc)
}

// Unwrap unwraps the K8sCluster entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (kc *K8sCluster) Unwrap() *K8sCluster {
	tx, ok := kc.config.driver.(*txDriver)
	if !ok {
		panic("ent: K8sCluster is not a transactional entity")
	}
	kc.config.driver = tx.drv
	return kc
}

// String implements the fmt.Stringer.
func (kc *K8sCluster) String() string {
	var builder strings.Builder
	builder.WriteString("K8sCluster(")
	builder.WriteString(fmt.Sprintf("id=%v", kc.ID))
	builder.WriteString(", created_at=")
	builder.WriteString(kc.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", updated_at=")
	builder.WriteString(kc.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", deleted_at=")
	builder.WriteString(kc.DeletedAt.Format(time.ANSIC))
	builder.WriteString(", name=")
	builder.WriteString(kc.Name)
	builder.WriteString(", agentClusterId=")
	builder.WriteString(fmt.Sprintf("%v", kc.AgentClusterId))
	builder.WriteByte(')')
	return builder.String()
}

// K8sClusters is a parsable slice of K8sCluster.
type K8sClusters []*K8sCluster

func (kc K8sClusters) config(cfg config) {
	for _i := range kc {
		kc[_i].config = cfg
	}
}
