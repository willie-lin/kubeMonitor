// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/willie-lin/kubeMonitor/pkg/nextserver/database/ent/k8spod"
)

// K8sPod is the model entity for the K8sPod schema.
type K8sPod struct {
	config `json:"-"`
	// ID of the ent.
	ID uint `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt time.Time `json:"deleted_at,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Qos holds the value of the "qos" field.
	Qos string `json:"qos,omitempty"`
	// K8sClusterId holds the value of the "k8sClusterId" field.
	K8sClusterId uint `json:"k8sClusterId,omitempty"`
	// K8sNamespaceId holds the value of the "k8sNamespaceId" field.
	K8sNamespaceId uint `json:"k8sNamespaceId,omitempty"`
	// K8sObjectId holds the value of the "k8sObjectId" field.
	K8sObjectId uint `json:"k8sObjectId,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*K8sPod) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case k8spod.FieldID, k8spod.FieldK8sClusterId, k8spod.FieldK8sNamespaceId, k8spod.FieldK8sObjectId:
			values[i] = new(sql.NullInt64)
		case k8spod.FieldName, k8spod.FieldQos:
			values[i] = new(sql.NullString)
		case k8spod.FieldCreatedAt, k8spod.FieldUpdatedAt, k8spod.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type K8sPod", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the K8sPod fields.
func (kp *K8sPod) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case k8spod.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			kp.ID = uint(value.Int64)
		case k8spod.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				kp.CreatedAt = value.Time
			}
		case k8spod.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				kp.UpdatedAt = value.Time
			}
		case k8spod.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				kp.DeletedAt = value.Time
			}
		case k8spod.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				kp.Name = value.String
			}
		case k8spod.FieldQos:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field qos", values[i])
			} else if value.Valid {
				kp.Qos = value.String
			}
		case k8spod.FieldK8sClusterId:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field k8sClusterId", values[i])
			} else if value.Valid {
				kp.K8sClusterId = uint(value.Int64)
			}
		case k8spod.FieldK8sNamespaceId:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field k8sNamespaceId", values[i])
			} else if value.Valid {
				kp.K8sNamespaceId = uint(value.Int64)
			}
		case k8spod.FieldK8sObjectId:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field k8sObjectId", values[i])
			} else if value.Valid {
				kp.K8sObjectId = uint(value.Int64)
			}
		}
	}
	return nil
}

// Update returns a builder for updating this K8sPod.
// Note that you need to call K8sPod.Unwrap() before calling this method if this K8sPod
// was returned from a transaction, and the transaction was committed or rolled back.
func (kp *K8sPod) Update() *K8sPodUpdateOne {
	return (&K8sPodClient{config: kp.config}).UpdateOne(kp)
}

// Unwrap unwraps the K8sPod entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (kp *K8sPod) Unwrap() *K8sPod {
	tx, ok := kp.config.driver.(*txDriver)
	if !ok {
		panic("ent: K8sPod is not a transactional entity")
	}
	kp.config.driver = tx.drv
	return kp
}

// String implements the fmt.Stringer.
func (kp *K8sPod) String() string {
	var builder strings.Builder
	builder.WriteString("K8sPod(")
	builder.WriteString(fmt.Sprintf("id=%v", kp.ID))
	builder.WriteString(", created_at=")
	builder.WriteString(kp.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", updated_at=")
	builder.WriteString(kp.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", deleted_at=")
	builder.WriteString(kp.DeletedAt.Format(time.ANSIC))
	builder.WriteString(", name=")
	builder.WriteString(kp.Name)
	builder.WriteString(", qos=")
	builder.WriteString(kp.Qos)
	builder.WriteString(", k8sClusterId=")
	builder.WriteString(fmt.Sprintf("%v", kp.K8sClusterId))
	builder.WriteString(", k8sNamespaceId=")
	builder.WriteString(fmt.Sprintf("%v", kp.K8sNamespaceId))
	builder.WriteString(", k8sObjectId=")
	builder.WriteString(fmt.Sprintf("%v", kp.K8sObjectId))
	builder.WriteByte(')')
	return builder.String()
}

// K8sPods is a parsable slice of K8sPod.
type K8sPods []*K8sPod

func (kp K8sPods) config(cfg config) {
	for _i := range kp {
		kp[_i].config = cfg
	}
}
